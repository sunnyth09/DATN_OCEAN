<?php require __DIR__."/vendor/autoload.php"; $app = require_once __DIR__."/bootstrap/app.php"; $app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); $order = App\Models\Order::where("order_code", "ORD6A5A76E14C68819")->first(); $rule = App\Models\LoyaltyRule::findByKey("ORDER_COMPLETE"); $alreadyEarned = App\Models\LoyaltyTransaction::forUser($order->user_id)->where("rule_id", $rule->id)->where("reference_type", App\Models\Order::class)->where("reference_id", $order->order_id)->exists(); echo json_encode(["order_id" => $order->order_id, "grand_total" => $order->grand_total, "rule_id" => $rule->id, "alreadyEarned" => $alreadyEarned]);
