# Face Verification Service Configuration
# Ngưỡng distance cho phép (0.4 = rất nghiêm ngặt, 0.6 = lỏng)
FACE_TOLERANCE=0.45

# Số lượng encodings tối đa mỗi request
MAX_ENCODINGS_PER_REQUEST=10

# CORS allowed origins (comma-separated, để trống = không cho phép cross-origin)
ALLOWED_ORIGINS=
